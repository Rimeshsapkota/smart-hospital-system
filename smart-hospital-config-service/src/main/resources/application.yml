server:
  port: 8888

spring:
  profiles:
    include: native
  application:
    name: config-service
#  cloud:
#    config:
#      server:
#        native:
#          search-locations: classpath:/config/
#        enabled: true
eureka:
  client:
    enabled: true
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

