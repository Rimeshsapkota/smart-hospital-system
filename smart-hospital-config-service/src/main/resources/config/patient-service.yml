server:
  port: 8145
  servlet:
    context-path: /patient-service

spring:
  cloud:
    config:
      override-system-properties: false
      fail-fast: true
      retry:
        max-attempts: 20
        initial-interval: 10000
        max-interval: 15000