server:
  port: 8145

spring:
  cloud:
    config:
      override-system-properties: false
cloud:
  config:
    fail-fast: true
    retry:
      max-attempts: 20
      initial-interval: 10000
      max-interval: 15000

